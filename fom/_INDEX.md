# fom/ — Figure of Merit (cross-cutting integration)

**Empty for now.** Postponed from W1 Day 5 to W2.

Planned content:
- `fom_inputs.yaml` — single contract every team writes their outputs into
- `fom_README.md` — what each block means + which team owns it
- `fom_validate.py` — schema check, screams when a required field is missing
- `wfom_engine.py` — scoring against CAREM-25 / SMART / VOYGR (per `planning/PLAN.md`)

Upstream design: `planning/PLAN.md`, `planning/MEETING_BRIEF.md`.
Cross-refs from: every `fom_param` field in `safety/safety_criteria.yaml`.
